/* ModuloDashboard — removido na v4; a tela de início é renderizada por App._renderizarDashboard() */
/* Este arquivo mantém apenas o stub para evitar erros de referência. */
const ModuloDashboard = { renderizar() {} };
